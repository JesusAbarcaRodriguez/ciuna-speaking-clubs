# Cadena de conexión de Supabase (Project Settings > Database > Connection string).
# Usa el "Transaction pooler" (puerto 6543) para entornos serverless como Vercel.
DATABASE_URL=postgresql://postgres.xxxxx:PASSWORD@aws-0-region.pooler.supabase.com:6543/postgres

# Supabase Auth (Project Settings > API). El anon key es público por diseño,
# está protegido por las reglas de Supabase Auth, no es un secreto.
PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=cambia-esto
